= form_for @tag_context, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :label, :class => 'control-label'
    .controls
      = f.text_field :label, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), tag_contexts_path, :class => 'btn'
