.nested-fields
  = f.inputs do
    = link_to_remove_association "Remove #{@tag_context.label}", f, :render_options =>{:wrapper=>:bootstrap}
    = f.input :label, :required=>true, :input_html => { :size => 20 }
    = f.input :teaser, :required=>false, :input_html => { :size => 80 }
    = f.input :writeup, :required=>false
